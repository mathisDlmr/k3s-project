apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tutut-storage-pvc
  namespace: tutut
  labels:
    app: tutut
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "local-path" # TODO : change to "cephfs"
  resources:
    requests:
      storage: 5Gi
